# Claude compatibility

Use Claude's native instruction and skill discovery. Invoke a shared skill with
`/skill-name` when available. Do not substitute a Claude subagent when the user
specifically asks for a Codex review. Native permissions remain authoritative.
