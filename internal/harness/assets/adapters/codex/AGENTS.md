# Codex compatibility

Use Codex's native instruction and skill discovery. Invoke a shared skill with
`$skill-name` when available. Do not substitute a Codex subagent when the user
specifically asks for a Claude review. Collect yielded command results before
claiming completion. Native permissions remain authoritative.
